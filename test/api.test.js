const test=require('node:test');const assert=require('node:assert/strict');let server,base;test('health endpoint responds',async t=>{const app=require('../src/server');server=app.listen(0);await new Promise(r=>server.once('listening',r));base=`http://127.0.0.1:${server.address().port}`;const r=await fetch(base+'/health');assert.equal(r.status,200);const d=await r.json();assert.equal(d.status,'ok');t.after(()=>server.close())});test('unknown API route returns JSON 404',async()=>{const r=await fetch(base+'/achievements/nope/nope');assert.equal(r.status,404);const d=await r.json();assert.equal(d.success,false)});test('signup validates SRM email and registration number',async()=>{const r=await fetch(base+'/auth/signup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:'Test',reg_number:'BAD',email:'test@gmail.com',password:'password123',confirm_password:'password123'})});assert.equal(r.status,400);const d=await r.json();assert.equal(d.success,false)});