const express=require('express');const db=require('../db');const {authRequired}=require('../middleware/auth');const router=express.Router();const VALID_SPORTS=['Cricket','Football','100m Dash','500m Dash','Shot Put','Long Jump','Chess','Carrom','Volleyball','Hockey'];const VALID_LEVELS=['School Level','College Level','Inter-College','District Level','State Level','National Level','International Level'];const VALID_CATEGORIES=['Individual','Team','Mixed'];const MAX={student_name:100,roll_number:30,batch_year:20,class:50,achievement:300,position:80,score:100,event_name:200,event_date:10,description:2000};const clean=v=>typeof v==='string'?v.trim():'';function validatePayload(b){const data={student_name:clean(b.student_name),roll_number:clean(b.roll_number).toUpperCase(),batch_year:clean(b.batch_year),class:clean(b.class),sport:clean(b.sport),category:clean(b.category),achievement:clean(b.achievement),position:clean(b.position),score:clean(b.score),event_name:clean(b.event_name),event_date:clean(b.event_date),level:clean(b.level),description:clean(b.description)};for(const f of ['student_name','roll_number','class','sport','category','achievement','level'])if(!data[f])return{error:`${f.replace(/_/g,' ')} is required`};if(!/^[A-Z0-9]{15}$/.test(data.roll_number))return{error:'Registration number must be exactly 15 letters/numbers'};if(!VALID_SPORTS.includes(data.sport))return{error:'Invalid sport'};if(!VALID_LEVELS.includes(data.level))return{error:'Invalid level'};if(!VALID_CATEGORIES.includes(data.category))return{error:'Invalid category'};if(data.event_date&&!/^\d{4}-\d{2}-\d{2}$/.test(data.event_date))return{error:'Invalid event date'};for(const[f,max]of Object.entries(MAX))if(data[f].length>max)return{error:`${f.replace(/_/g,' ')} is too long (max ${max} characters)`};return{data}}router.get('/',(req,res,next)=>{try{const{student_name,sport,level,class:cls}=req.query;let sql='SELECT * FROM achievements WHERE 1=1';const p=[];if(student_name){sql+=' AND (student_name LIKE ? OR roll_number LIKE ?)';const q=`%${student_name}%`;p.push(q,q)}if(sport){sql+=' AND sport = ?';p.push(sport)}if(level){sql+=' AND level = ?';p.push(level)}if(cls){sql+=' AND class LIKE ?';p.push(`%${cls}%`)}sql+=' ORDER BY id DESC';const rows=db.prepare(sql).all(...p);res.json({success:true,count:rows.length,data:rows})}catch(e){next(e)}});router.get('/:id',(req,res,next)=>{try{const id=Number(req.params.id);if(!Number.isInteger(id)||id<1)return res.status(400).json({success:false,error:'Invalid achievement id'});const row=db.prepare('SELECT * FROM achievements WHERE id=?').get(id);if(!row)return res.status(404).json({success:false,error:'Not found'});res.json({success:true,data:row})}catch(e){next(e)}});router.post('/',authRequired,(req,res,next)=>{try{const v=validatePayload(req.body||{});if(v.error)return res.status(400).json({success:false,error:v.error});const b=v.data;const r=db.prepare('INSERT INTO achievements(student_name,roll_number,batch_year,class,sport,category,achievement,position,score,event_name,event_date,level,description,created_by) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)').run(b.student_name,b.roll_number,b.batch_year,b.class,b.sport,b.category,b.achievement,b.position,b.score,b.event_name,b.event_date,b.level,b.description,req.user.reg_number);const row=db.prepare('SELECT * FROM achievements WHERE id=?').get(r.lastInsertRowid);res.status(201).json({success:true,message:'Achievement recorded!',data:row})}catch(e){next(e)}});router.put('/:id',authRequired,(req,res,next)=>{try{const id=Number(req.params.id);if(!Number.isInteger(id)||id<1)return res.status(400).json({success:false,error:'Invalid achievement id'});if(!db.prepare('SELECT id FROM achievements WHERE id=?').get(id))return res.status(404).json({success:false,error:'Not found'});const v=validatePayload(req.body||{});if(v.error)return res.status(400).json({success:false,error:v.error});const b=v.data;db.prepare('UPDATE achievements SET student_name=?,roll_number=?,batch_year=?,class=?,sport=?,category=?,achievement=?,position=?,score=?,event_name=?,event_date=?,level=?,description=?,updated_at=datetime(\'now\') WHERE id=?').run(b.student_name,b.roll_number,b.batch_year,b.class,b.sport,b.category,b.achievement,b.position,b.score,b.event_name,b.event_date,b.level,b.description,id);res.json({success:true,message:'Updated',data:db.prepare('SELECT * FROM achievements WHERE id=?').get(id)})}catch(e){next(e)}});router.delete('/:id',authRequired,(req,res,next)=>{try{const id=Number(req.params.id);if(!Number.isInteger(id)||id<1)return res.status(400).json({success:false,error:'Invalid achievement id'});if(!db.prepare('SELECT id FROM achievements WHERE id=?').get(id))return res.status(404).json({success:false,error:'Not found'});db.prepare('DELETE FROM achievements WHERE id=?').run(id);res.json({success:true,message:'Deleted'})}catch(e){next(e)}});module.exports=router;